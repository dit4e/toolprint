"""mcpdrift - offline MCP assessment tool."""

# The public name is undecided (see PLAN.md section 8). It is spelled here and in
# cli.py's argparse prog only, so renaming stays a two-line change.
TOOL_NAME = "mcpdrift"
__version__ = "0.1.0"
